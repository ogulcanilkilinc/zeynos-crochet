[build]
  publish = "."
  command = "echo 'Static HTML site build successful'"

[build.environment]
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  DISABLE_NEXTJS_PLUGIN = "true"

[[headers]]
  for = "/*"
    [headers.values]
      Cache-Control = "public, max-age=0, must-revalidate"
